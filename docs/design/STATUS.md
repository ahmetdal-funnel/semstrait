# Spec-Driven Development — Status

Living session-handoff file. Updated at the end of each working session (agent proposes; human approves).

**Authoritative spec root**: [`00_overview.md`](00_overview.md). Read `00_overview.md` first, then this file.

---

## 1. Current phase

**Phase**: Reconciliation — **model + entities surface closed end-to-end; documentation consolidation landed (eighth pass, 2026-04-17); questions/ directory restructure landed (ninth pass, 2026-04-27); metadata-derived Dimension authoring surface ratified (tenth pass, 2026-04-27) — `[TD-MAP-METADATA-FOLD]` resolved**

The first-pass drafting of all numbered documents under `docs/design/` is complete. The YAML-root shape and the in-memory `SemanticModel` type hierarchy landed as a ratified Phase-2 decision in `apis/32_semstrait_model.md` (and `apis/32b_catalogs_yaml.md` for the sibling catalogs file) on 2026-04-17. The nesting matrix landed in `data-kinds/26_nesting_matrix.md`. A follow-up pass on the same session stripped decision-log content from those three docs and slimmed the reconciliation markers on every pre-dating doc to a single pointer line. A third pass on the same session refactored the DataKind hierarchy from bare structs to a sealed-trait shape on two orthogonal axes (structural + behavioral), restoring explicit "is-a" relationships. A sixth pass on the same session **closed the canonical entity types end-to-end** — 18 ratifications covering `Relationship`, `TemporalShape`, `Dimension` / `Measure` / `Metric`, filter taxonomy, `AiContext`, `Keys`, `SemanticMapping` values, the `CatalogRef` bare-alias decision, the `ComplexDataKind` ≥ 2 children rule, and the cross-DataKind root-metric and implicit-Joinset-exposure decisions — consolidated into the new `apis/32c_entities.md` and cascaded across `32`, `32b`, `26`. **A seventh pass on the same session (2026-04-17) completed the Phase-3 mechanical cascade** — strengthened reconciliation pointers on every pre-dating doc (`15`, `17`, `20`–`25`, `11`, `12`, `14b`, `16`, `10`, `00`, `registry/README`), applied the `ColumnMapping` → `SemanticMapping` rename at the model-layer surface (with the 4-variant → 3-variant roster change flagged as `[TD-MAP-METADATA-FOLD]`), annotated SCD `Type0` / `Type3` / `Type4` / `Type5` / `Type6` as post-v1 deferred, and retired seven auto-closed open questions (`Q-UNI-002`, `Q-UNI-009`, `Q-GRN-004`, `Q-GRN-006`, `Q-TEMPORAL-002`, `Q-TEMPORAL-005`, `Q-TEMPORAL-006`).

The current working principle remains: **describe, clean, and name what the code actually implements; do not fabricate parallel models**.

**Phase map**:

| Phase | Scope | State |
|---|---|---|
| Phase 0 | First-pass drafting of all numbered spec docs | Complete |
| Phase 1 | Ground-truth audit: per-doc delta list vs. current code | Partial — items A / B / D / E surfaced; full matrix pending |
| Phase 2 | Scoped Q&A rounds on genuinely-open design items | **Partial — root shape + catalogs + canonical entities ratified (32 / 32b / 26 / 32c); constraints deferred (see §3); adapter/catalog framing (C) pending** |
| Phase 3 | Apply Phase-1 deltas + Phase-2 decisions across the doc set | **Complete for the model + entities surface** — reconciliation pointers + targeted vocabulary updates landed across every pre-dating doc; seven auto-closed open-Qs annotated; the previously-flagged `[TD-MAP-METADATA-FOLD]` follow-up was **resolved on 2026-04-27** (full ratification of metadata-derived Dimension authoring surface; `Metadata` restored as 4th `SemanticMappingValue` variant, v1 path-only scope, three-stratum architecture documented across `15`/`13`/`16`/`18`/`15_questions.md`). Per-variant data-kinds docs (`21`–`24`) have updated pointers but retain pre-`32c` body text as historical reference; full body rewrites are post-ratification polish. |
| Consolidation | Documentation-structure optimization: single-source-of-truth audit, duplicate purge, index & precedence rule | **Complete (eighth pass, 2026-04-17)** — `apis/32c_entities.md` promoted to `foundations/18_entities.md`; duplicate entity struct / enum blocks in `16`, `17`, `15`, `13`, `14b`, `33`, `32` eviscerated; `17 §2.3` pre-consolidation Rust sketch for `TemporalShape` / `ScdSubtype` removed; `18 §3.5` `Grain` duplicate (that had silently added `Second`) replaced with a pointer to canonical home in `13 §3.1`; `16 §5.2` `RelationshipPath` struct converted to a pointer to `14b §4.5`; `32`'s `refined-by:` trimmed to downstream-only per directionality rule; 3 registry open-Q sidecars folded into `questions/open/registry_questions.md`; `joinset_shape_semantics.md` folded into `24_questions.md §Post-v1 shape-hint clusters`. New `docs/design/INDEX.md` provides scan-optimized topic → canonical-doc lookup + folder map + duplication guard + crate-surface mirroring convention. New `00 §4.4` precedence rule ratifies conflict-resolution order (authoritative-for → directionality → glossary). |
| Restructure | Questions-directory restructure: rename `open_questions/` → `questions/{open,closed}/`; drop redundant `_open_questions.md` filename suffix | **Complete (ninth pass, 2026-04-27)** — 28 active sidecars moved to `questions/open/`; 1 fully-resolved sidecar (`31b_io_questions.md`) moved to `questions/closed/`; mechanical reference sweep updated every path-prefixed link, every bare filename reference, and every front-matter `doc:` field across `docs/design/` + `CLAUDE.md`; INDEX.md folder map + open-questions section split into per-directory tables; STATUS.md + INDEX.md "Renames landed" log updated. Zero content changes inside any sidecar (Q-IDs, bodies, status summaries unchanged). Goal: visibility — at-a-glance separation between active backlog (`open/`) and historical record (`closed/`). |
| Finalization | Cross-doc consistency pass, ratified spec freeze | Not started |

---

## 2. Active reconciliation items

Six themes surfaced by the ground-truth audit plus two that landed as new ratifications in the late-April session. As of 2026-04-17, the YAML-root-shape axes (A + D) are ratified in `apis/32_semstrait_model.md`, the catalogs file surface is ratified in `apis/32b_catalogs_yaml.md`, the nesting matrix is ratified in `data-kinds/26_nesting_matrix.md`, the I/O transport is ratified in `apis/31b_semstrait_core_io.md`, and the **canonical entity types** are ratified in the new `apis/32c_entities.md`. Short reconciliation pointers sit at the top of every Phase-0 doc whose framing pre-dates those specs.

| Item | Summary | Status | Primary docs affected |
|---|---|---|---|
| **A. YAML surface** | Top-level uses per-variant plural tags: `datasets:`, `grainsets:`, `unionsets:`, `joinsets:` (no unified `data_kinds:` parent, no `kind:` discriminator). `SemanticModel` holds per-variant `BTreeMap<String, Dataset>` etc. Concrete types split into `Public*` (top-level, carry `SemanticInterface` + `ai_context`) and `Nested*` (structural shell, body only). Both forms of a variant wrap the same `*Body` struct; common fields live in `DataKindBase`; "is-a" is expressed through a sealed trait hierarchy (`DataKind` base; structural axis `SimpleDataKind` / `ComplexDataKind`; behavioral axis `PublicDataKind` / `NestedDataKind`); cross-variant iteration uses five view enums. | **Ratified (32 §1–§3, 32 §6–§7, 32b)** — cascade rewrite of `20`–`25` / `33` / `15` / `10` pending; pointers in place. | `32` / `32b` / `26` (clean specs); `20`–`25`, `10`, `15`, `33` (pointers) |
| **B. `Binding` → `SemanticMapping`** | `Binding` as a model-layer type does not exist; the ratified shape is: `semantic_mapping:` lives in `extras`, is effective only at leaves, defaults from ancestors, and its value is **one of three author-facing variants** — `Column(String)`, `Literal(LiteralValue)`, `Expr(PhysicalExpr)` — plus a **compile-synthesized fourth variant** `Metadata(MetadataDimensionRecipe)` (not authored under `semantic_mapping:`; produced by the compiler from a Dimension's `type: { metadata: ... }` block per `15 §5.5` / `15 §8`). The `SemanticMappingValue` enum is therefore 4-variant at the type level (`18 §10`). `Binding` is the compile-time process that consumes the mapping + resolved `PhysicalSource`s. | **Ratified at authoring level (32 §4, §5, 18 §10)** — the compile-time algorithm in `15` is now ratified against `SemanticMapping` vocabulary; the cascade-pending consumers below are still being trimmed for redundancy. | `15_mapping_and_binding.md`; `32`, `18`, `33`; `14b`, `21` (cascade in progress) |
| **C. Adapter / Catalog framing** | `semstrait-adapter` and `semstrait-catalog` are single crates with **feature-gated modules** (engines: `datafusion` / `duckdb` / `spark`; providers: `local` / `iceberg` / `unity` / `aws`). Not per-engine/per-provider crates. | **Not yet ratified** — deferred to a dedicated session; docs still carry Phase-0 framing. | `36_semstrait_adapter.md`, `37_semstrait_catalog.md`, `39_semstrait_facade.md`, `42_migration_notes.md` |
| **D. "Dataset" vs "Simple" spelling** | Ratified: authoring-level variant name is `Dataset` (YAML tag `datasets:`); Rust concrete leaf types are `Dataset` (Public) and `NestedDataset` (Nested), both wrapping `DatasetBody`; `SimpleDataKind` is the sealed marker trait that both concrete leaf forms implement. | **Ratified (32 §3)** — cascade rewrite pending; pointers in place. | `20`–`25`, `32`, `33` (pointers) |
| **E. Constraints model** | **Deferred — see §3**. | Deferred. | `11 §8`, `10 §3.4`, `13 §5.3`, `32`, `32c` |
| **F. Same-variant self-nesting + nested-form structural-only rule + ≥ 2-children rule** | R1 (leaves don't nest) + R2 (same-variant self-nesting banned) + R3 (ComplexDataKind ≥ 2 children) ratified; nested forms carry no `semantic_interface` / `ai_context`. | **Ratified (26 §2 + 32 §3.2–§3.4 + 32 SR-2 / SR-4 / SR-10)** — joinset shape hints + associativity parked in [`questions/open/24_questions.md §Post-v1 shape-hint clusters`](questions/open/24_questions.md#post-v1-shape-hint-clusters-folded-in-2026-04-17) (folded from the retired `joinset_shape_semantics.md` sidecar on 2026-04-17). | `26` (clean spec), `32`, pointers on `22`–`24` |
| **G. I/O transport layer** | `semstrait-core::io` owns byte-blob transport primitives (`Source` / `Sink` traits with generic `read<T: FromIoBytes>` / `write<B: IntoIoBytes>` default methods, `Location` enum, `IoError`, back-ends `memory` / `local` / `s3`) behind `io` (default ON) and `io-aws` (default OFF) features. All back-ends thin-wrap `object_store` (Apache Arrow) — never re-exported publicly except via the one `S3SourceBuilder::with_object_store_builder` escape hatch. Domain load / dump wrappers (`load_model`, `dump_model`, `load_manifest`, …) live in their owning crates (`model::io`, `manifest::io`). Supersedes TD-008's "new `semstrait-io` crate" plan. Concurrent-write is LWW; client cache keyed by `(region, endpoint)` for `Location::from_str`; error enums are `#[non_exhaustive]` top-to-bottom. | **Ratified — all open questions closed (31b, 31b_io_questions, 31 §2 / §11 / §12, 32 §10.4, 32b §5.4, 33 §16.5)** — migration tracked as `[TD-008-MIGRATE]`. | `31b` (new spec, closed-out), `31` / `32` / `32b` / `33` (updated), `31b_io_questions.md` (all 5 parked + 10 uncovered items closed), `docs/TECH_DEBT.md` (amended) |
| **H. Canonical entity types** | **New — landed 2026-04-17 (late session).** 18 ratifications consolidated into `apis/32c_entities.md`: (1) unified `Relationship` struct at root + Joinset sites with `join_type` / `keys` hybrid + residual `filter` / `cardinality` required / `directionality` default bidirectional; (2) `JoinType` roster `{Inner, Left, Right, Full}`, `AsOf` deferred; (3) shared Semantics pools at root (`dimensions:` / `measures:` / `metrics:`) + per-DataKind inline-or-ref grammar with narrow overridable fields (`expr`, `filters`); (4) orphan policy — every Semantic physically binds at least once; (5) `TemporalShape` struct + `TemporalShapeKind` enum + per-variant `*Body`s; collapsed YAML wrapper; `grain:` required on leaf / forbidden on complex; Grainset children author explicit grain; `ScdType` trimmed to `{Type1, Type2}`; (6) filter taxonomy — `DataKindFilter` (pre-agg predicate, user-facing) vs `AggregationFilter` (conditional aggregation); no cross-ref; (7) `DimensionType` roster of six (`Temporal, Categorical, Binary, Geo, Bucketed, Metadata`); (8) `AggregationType` expanded to 9 variants incl. `Median`, `StdDev`, `Variance`; (9) `AdditivityType` `{Full, Semi{axes, strategy}, Non}`; (10) `Measure.agg:` required; `Metric.expr:` required; `data_type:` mandatory & immutable across levels; (11) `Keys` `{primary, unique, foreign}` w/ bare Semantic names; (12) `AiContext` trimmed to 4 fields (`synonyms`, `description`, `examples`, `unit`); (13) `SemanticMapping` value = 3 variants (`Column`, `Literal`, `Expr`); (14) `CatalogRef` bare-alias only — map-form + `namespace:` override dropped; (15) `partition_def` nested inside `StorageConfig` (not a top-level Extras field); (16) `ComplexDataKind` ≥ 2 children (SR-10); (17) Implicit planner-synthesised Joinsets are internal only — not surfaced as queryable entities; (18) Root-level Metric may cross DataKinds — planner uses the relationship graph. 12 new `SR-E-*` entity-level diagnostic codes. | **Ratified (32c + cascaded into 32 / 32b / 26)** — per-variant cascade into `21` / `22` / `23` / `24` pending. | `32c` (new spec), `32` / `32b` / `26` (amended), `21`–`24`, `11`, `13`, `14b`, `15`, `16`, `17` (pending cascade) |

---

## 3. Deferred topics

### 3.1 Constraints design

**Status**: Deferred to a dedicated session.

**Frozen context**: [`questions/open/11_constraints_deferred.md`](questions/open/11_constraints_deferred.md) — contains the full in-flight Q-R4 thread (ratified axis, open shape questions, last concrete example, and three specific ratification items).

**Resume from**: the three open ratification items captured in `11_constraints_deferred.md` — `aggregation:` sub-block semantics, key-naming (`aggregation` vs `aggregations`, `all` vs `all_of`), and whether `constraints.filter:` sub-blocks or `Filter.constraints` entity-level fields (or both) are in scope for v1.

**Do not** proceed with a fourth rewrite of `11 §8` until this session lands.

---

## 4. Open Q&A rounds per document

Per-document open-question files live under [`questions/open/`](questions/open/). Index:

| Doc | Open-questions file | Notable themes |
|---|---|---|
| 11 (names & scopes) | `questions/open/11_constraints_deferred.md` (snapshot) | Constraints — deferred |
| 14b (expression resolution) | `questions/open/14b_questions.md` | Cross-DataKind path pre-resolution |
| 15 (mapping & binding) | `questions/open/15_questions.md` | Blocked on item B (SemanticMapping) |
| 16 (composition) | `questions/open/16_questions.md` | Relationship-driven composition edges |
| 17 (temporal shape) | `questions/open/17_questions.md` | 14 deferred items, incl. AsOf planner impl |
| 20–25 (data-kinds) | `questions/open/{20..25}_questions.md` | 37 questions total, all blocked on item A + D |
| 30 (API contracts) | `questions/open/30_questions.md` | Error-code range finalization |
| 31 (core) | `questions/open/31_questions.md` | Public-surface scope |
| 31b (core::io) | `questions/closed/31b_io_questions.md` | **All resolved — historical record only** |
| 32 (model) | `questions/open/32_questions.md` | YAML surface — item A ratified; canonical entities (item H) ratified in `32c`; Q-MODEL-001 partially resolved; Q-003/004 retired; model-shape stale items retired in the late-session close-out |
| 32c (entities) | — | Canonical entity types — `#[non_exhaustive]` on every struct / enum per I10; no open items at session close; per-variant YAML cascade into `21`–`24` is a Phase-3 mechanical task, not a Q&A item |
| 33 (manifest) | `questions/open/33_questions.md` | Storage, repository, integrity |
| 34 (planner) | `questions/open/34_questions.md` | Strategy dispatch, optimizer API |
| 35 (IR) | `questions/open/35_questions.md` | Substrait round-trip, visitor API |
| 36 (adapter) | `questions/open/36_questions.md` | Blocked on item C (module framing) |
| 37 (catalog) | `questions/open/37_questions.md` | Blocked on item C (module framing) |
| 38 (api) | `questions/open/38_questions.md` | Unified builder, warning propagation |
| 39 (facade) | `questions/open/39_questions.md` | Feature-flag table — blocked on item C |
| 40 (refactor plan) | `questions/open/40_questions.md` | Phased rollout decisions |
| 41 (deprecations) | `questions/open/41_questions.md` | Tombstone horizons, alias mechanism |
| 42 (migration notes) | `questions/open/42_questions.md` | Recipe rendering, per-MAJOR ordering |
| registry/functions | `questions/open/functions_mapping_questions.md` | Per-engine function parity |
| registry/temporal | `questions/open/temporal_shape_mapping_questions.md` | AsOf rewrite-tier matrix |
| registry/joins | `questions/open/join_types_mapping_questions.md` | Cardinality-informed emission |

---

## 5. Last checkpoint

**Session**: 2026-04-17 (YAML-root ratification + spec-cleanup + DataKind trait-hierarchy session + `semstrait-core::io` transport ratification + canonical-entity types end-to-end closure)

**Accomplished**:

- Ratified the YAML root shape, the in-memory `SemanticModel` type hierarchy, and the `Extras` block via extensive Q&A. Final shape:
  - `semantic_model:` root wrapper with per-variant plural tags (`datasets:`, `grainsets:`, `unionsets:`, `joinsets:`); shared pools (`dimensions:`, `measures:`, `metrics:`); `relationships:` at model-level + joinset-level only.
  - Type hierarchy (six layers): `DataKindBase` common-fields struct → four `*Body` structs (one per variant, each holding `base: DataKindBase` + variant-intrinsic structural fields; self-nesting forbidden by field absence) → eight concrete types (`Public*` wrap body + `ai_context` + `SemanticInterface`; `Nested*` wrap body only) → sealed trait hierarchy rooted at `DataKind` with a structural axis (`SimpleDataKind` marker / `ComplexDataKind` with `allowed_child_variants` / `child_count` / `children_ref`) and a behavioral axis (`PublicDataKind` with `ai_context` / `semantic_interface` / `NestedDataKind` marker) → per-concrete trait impls → five view enums (`AnyDataKindRef`, `PublicDataKindRef`, `NestedDataKindRef`, `SimpleDataKindRef`, `ComplexDataKindRef`) for heterogeneous iteration, each implementing the subset of the trait hierarchy common to its members.
  - Unified `Extras` struct with fields: `catalog`, `storage`, `semantic_mapping`, `temporal` (initial draft had a top-level `partition_def`; the sixth pass nested it inside `StorageConfig` — see sixth-pass entry below). `UnionMode` is a direct field on `UnionsetBody` (not in extras).
  - Same-variant self-nesting forbidden (R2); leaves never nest (R1); nested forms carry no `SemanticInterface` / `ai_context`.
  - `ColumnMapping` → `SemanticMapping` rename; `Binding` is a compile-time process, not a model-layer type; `physical_source` stays manifest-level only; `JoinAssociativity` dropped; `namespace` dropped from `SemanticModel`.
- Wrote ratified spec docs (pure design definitions — no decision logs, status banners, or open-item lists):
  - `apis/32_semstrait_model.md` — root YAML contract + `SemanticModel` root type + the six-layer hierarchy + `Extras` + SR-* + `parse` API + `ParseError` roster.
  - `apis/32b_catalogs_yaml.md` — `catalogs.yaml` grammar + `CatalogEntry` + `CatalogAuthMethod` (oauth2 / bearer / aws_secrets) + `CatalogRef` reference site + `parse_catalogs` loader. Examples use `polaris_prod` / `polaris_dev` / `iceberg_local` aliases sourced from `test_data/catalogs.yaml`.
  - `data-kinds/26_nesting_matrix.md` — matrix + R1 / R2 + nested-form structural-only rule + dotted-plural addressing scheme + enforcement table (updated to reference `DatasetBody`, `*Body` child-field sets, `Nested*` field omissions, and the `SimpleDataKind` / `NestedDataKind` marker traits).
  - `questions/open/24_questions.md` — parked `JoinAssociativity` (Cluster A) + star/snowflake/3NF shape hints (Cluster B). *(Folded into `questions/open/24_questions.md §Post-v1 shape-hint clusters` on 2026-04-17 as Q-24-09 + Q-24-10; the standalone sidecar was retired in the same pass. See `24_questions.md` for the authoritative bodies.)*
- Second pass in the same session (post-feedback cleanup):
  - Replaced the earlier `BodyType<Public, Nested>` umbrella-enum proposal with concrete `Public*` / `Nested*` structs.
  - Reshaped `ComplexDataKind` from a single struct with child vectors into an enum whose arms each hold a variant-specific body.
  - Stripped all decision-log / rationale / "Status" / "Divergence" / "Open items" scaffolding from `32`, `32b`, `26`.
  - Slimmed the verbose multi-line reconciliation banners on pre-dating docs (`10`, `15`, `20`–`25`, `33`) to single-line pointer notes.
- Third pass in the same session (restore "is-a" relationships in the type system):
  - Retired the bare-struct hierarchy (`DataKind` struct / `SimpleDataKind` struct / `ComplexDataKind` enum) because it only expressed `has-a` composition — a caller could not say "a `Dataset` is a `DataKind`" in a generic bound.
  - Introduced a sealed trait hierarchy rooted at `DataKind` with two orthogonal axes: structural (`SimpleDataKind` marker / `ComplexDataKind` with accessor methods) and behavioral (`PublicDataKind` with `ai_context` / `semantic_interface` / `NestedDataKind` marker). Each concrete type implements `DataKind`, exactly one structural-axis trait, and exactly one behavioral-axis trait (eight concretes × three traits each).
  - Split the old universal `DataKind` struct into `DataKindBase` (common fields struct, held inside each body) and the `DataKind` trait (interface vocabulary). Common fields (`name`, `description`, `extras`) live on `DataKindBase`; the trait hands out accessors over them.
  - Added five view enums for heterogeneous iteration (`AnyDataKindRef`, `PublicDataKindRef`, `NestedDataKindRef`, `SimpleDataKindRef`, `ComplexDataKindRef`); each view implements the subset of the trait hierarchy common to its members and dispatches via `match`. `PublicDataKindRef`'s inline definition was removed from `32 §2.2` and folded into the new `32 §3.6` view-enum section; `32 §2.2` retains only the iterator signatures (`iter_all` / `iter_public` / `iter_simple` / `iter_complex` + `find_public`).
  - Added `ComplexDataKind::children_ref()` so composers expose a heterogeneous iterator over their `NestedDataKindRef<'_>` children.
- Fourth pass in the same session (ratified I/O transport placement):
  - Recognised that domain-level load / dump wrappers (`load_model`, `dump_model`, `load_manifest`, …) were landing ad-hoc in `semstrait-manifest::io` (one-off `load_text`) and had no stable home, while TD-008's original plan of "new `semstrait-io` crate" adds a workspace edge without additional isolation.
  - Ratified the placement: `semstrait-core::io` owns **transport-only** primitives (`Source` / `Sink` async traits, `Location` enum implementing both, `IoError`, back-ends `backends::memory` / `backends::local` / `backends::s3`); domain load / dump wrappers live in the owning crate (`model::io`, `manifest::io`, future adapter-bundle export). Core's zero-runtime-dep invariant is preserved under `--no-default-features`; `io` is default-on and pulls `tokio`; `io-aws` is default-off and pulls `aws-sdk-s3` / `aws-config`. `Location` implements `Source` and `Sink` so call-sites can say `Location::from_str("s3://bucket/key")?.load_text().await?` as a one-liner.
  - Authored [`apis/31b_semstrait_core_io.md`](apis/31b_semstrait_core_io.md) as the full spec: `Source` / `Sink` contracts, `Location` scheme-dispatch roster, `IoError` stable codes (`io.not_found`, `io.permission_denied`, `io.network`, `io.unsupported`, `io.malformed`), back-end semantics (local atomic temp-file + rename; S3 default credential chain), feature flags, WASM posture (memory-only), `no_std` posture (disable `io` feature), usage patterns, and structural rules (SR-IO-*).
  - Amended `31 §1.2 / §1.3 / §2 / §11 / §12 / §13 / §14.11 / §15 (R13)` to declare the `io` module, the `tokio` / AWS SDK dependency shape, and the invariant amendments (I6 — async permitted only in `io`; I11 — transport in core, domain wrappers elsewhere).
  - Amended `32 §10.1 / §10.4 / §10.5 / §11` — clarified "no *direct* I/O in parse" (parse is still `fn(&str)`); added §10.4 `Model-Level I/O Surface (semstrait-model::io)` with async `load_model` / `dump_model` / `load_catalogs` / `dump_catalogs` over `core::io::Source` / `Sink`, `DumpMode::Canonical`, and the four error types (`ModelLoadError`, `ModelDumpError`, `CatalogsLoadError`, `CatalogsDumpError`) with stable kebab-case codes; §10.5 declared new `io` / `io-aws` features forwarding to `core/io` and `core/io-aws`.
  - Amended `32b §5.4` — added shared `load_catalogs` / `dump_catalogs` wrappers (re-exported from `semstrait-model::io`; `catalogs` crate can also re-export them) to keep the surface DRY across model and catalogs.
  - Amended `33 §16.2 / §16.3 / §16.4 / §16.5` — declared the `semstrait-manifest::io` convenience submodule (`load_manifest` / `dump_manifest` one-shot free functions composing `core::io` transport + manifest byte-encoding), noted its relationship to `Repository` (Repository = full persistence contract with content-addressable IDs; `manifest::io` = one-shot sugar), and tracked the removal of the legacy `load_text` helper under `[TD-008-MIGRATE]`.
  - Amended `docs/TECH_DEBT.md` TD-008 — marked as **resolved by design**; migration tracked as `[TD-008-MIGRATE]` with explicit steps (land `core::io` → add `model::io` → add `manifest::io` → migrate CLI / builder call sites → drop `semstrait-manifest::io::load_text` and AWS SDK deps from manifest's `Cargo.toml`).
  - Created [`questions/closed/31b_io_questions.md`](questions/closed/31b_io_questions.md) — parked five deferred items: `Q-IO-001` faithful-dump mode, `Q-IO-002` HTTP / GCS / Azure back-ends, `Q-IO-003` multi-file / directory loader, `Q-IO-004` WASM back-end constraints, `Q-IO-005` atomic compare-and-swap on `Sink`.
  - Updated `questions/open/32_questions.md` — `Q-MODEL-001` (multi-file loader) marked **partially resolved**: single-file `load_model` / `load_catalogs` ratified; multi-file deferred to `31b Q-IO-003`. `Q-MODEL-003` (`simple` vs `dataset`) and `Q-MODEL-004` (`DataKindRef::Inline` hoisting) **retired as stale** — both superseded by the ratified 6-layer hierarchy where per-variant plural tags and concrete `Public*` / `Nested*` types replaced the earlier `DataKind { kind: ... }` / `DataKindRef::Inline` sketches.
- Fifth pass in the same session (I/O open-questions close-out round):
  - Ran a Q&A round covering every remaining design-level ambiguity in the I/O layer (byte-vs-text transport, generic trait shape, library adoption, cancellation, size limits, concurrent-write semantics, client caching, credential override, error composition, `describe()` contract, YAML-only naming, wrapper ownership) plus all five previously parked items (`Q-IO-001` … `Q-IO-005`). Every item closed to a ratified state; no live items remain in `31b_io_questions.md`.
  - Adopted `object_store` (Apache Arrow project) as the **internal** back-end foundation — all three `memory` / `local` / `s3` back-ends become thin wrappers (~30 LOC each) over the corresponding `object_store::ObjectStore` impls. `object_store` is never re-exported publicly except for the one documented `S3SourceBuilder::with_object_store_builder` escape hatch (SR-IO-8). Chosen over hand-rolled `tokio::fs` + `aws-sdk-s3` (eliminates ~300 LOC of glue, gets retry / multipart / credential-chain / proxy support for free, aligns with DataFusion / Delta Lake ecosystem). Chosen over `opendal` (lighter dep graph; API more minimal; closer to Arrow ecosystem defaults).
  - Switched the transport primitive from text-only (`Source::read() -> String`) to byte-level (`Source::read_raw() -> Bytes`) plus a generic default method `Source::read<T: FromIoBytes>` that delegates to `read_raw` + a conversion trait. Symmetric `Sink::write<B: IntoIoBytes>`. UTF-8 validation centralized in `FromIoBytes for String`, emitting `IoError::Malformed`. SemanticManifest (binary) uses the `Bytes` path; model (YAML text) uses the `String` path. One required method per trait; everything else is default generic.
  - Pinned `describe()` semantics to **stable content-addressable identity** (not free-form log text): equal `describe()` ⇒ identical bytes (absent concurrent mutation). Consequences: `InMemory::new(name, bytes)` requires a user-picked stable name; `LocalFile::describe()` returns absolute path; callers responsible for canonicalization if they want cache hits across relative entries.
  - Pinned `Location::from_str` client-cache policy: process-global `OnceLock<DashMap<ClientKey, Arc<object_store::aws::AmazonS3>>>` keyed by `(region, endpoint_url)`, no eviction in v1. Custom-built S3 clients via `S3SourceBuilder` bypass the cache.
  - Pinned error composition: every domain wrapper enum (`ModelLoadError` / `ModelDumpError` / `CatalogsLoadError` / `CatalogsDumpError` / `SemanticManifestLoadError` / `SemanticManifestDumpError`) is `#[non_exhaustive]` with `Io(IoError)` as one variant; `IoError` is itself `#[non_exhaustive]`. Adding an `IoError` variant propagates as MINOR through the entire dependency chain.
  - Pinned v1 back-end roster: **Local, InMemory, S3 only**; frozen. GCS / Azure / HTTP are not in v1; each lands as an additive MINOR feature flag when a concrete caller asks (each is ~30 LOC given `object_store`'s coverage).
  - Closed five parked items to ratified states: `Q-IO-001` **retired** (canonical-only dump; faithful reopens only on concrete LSP / refactor demand); `Q-IO-002` **retired for v1** (extras wait for MINOR-on-demand); `Q-IO-003` **closed forever** (multi-file walks are caller territory, not transport territory); `Q-IO-004` **closed for v1** (no wasm target); `Q-IO-005` **retired** (last-writer-wins in v1; CAS extension trait possible as MINOR).
  - Full rewrite of [`apis/31b_semstrait_core_io.md`](apis/31b_semstrait_core_io.md) — new §1.4 `Why object_store`, new §5 `FromIoBytes / IntoIoBytes`, revised §3 / §4 (byte-level primary method + generic default), revised §6 (`Location` + cache policy), revised §8 (`InMemory::new(name, bytes)` + `S3SourceBuilder`), ten SR-IO-* rules including SR-IO-8 (object_store is internal).
  - Full rewrite of [`questions/closed/31b_io_questions.md`](questions/closed/31b_io_questions.md) — every item resolved; file retained as historical record.
  - Updated [`apis/32_semstrait_model.md`](apis/32_semstrait_model.md) §10.4 — `load_model` now sugar over `src.read::<String>()`; noted `#[non_exhaustive]` propagation; updated "does NOT do" list for the closed parked items.
  - Updated [`apis/32b_catalogs_yaml.md`](apis/32b_catalogs_yaml.md) §5.4 — same `read::<String>()` adjustment; `#[non_exhaustive]` note.
  - Updated [`apis/33_semstrait_manifest.md`](apis/33_semstrait_manifest.md) §16.5 — manifest uses `read_raw()` (binary `Bytes`, not text); error enums shown as `#[non_exhaustive]`; stable codes expanded (`manifest.load.format-version`).
  - Updated [`apis/31_semstrait_core.md`](apis/31_semstrait_core.md) §11 / §12.1 / §14.11 / §15 (R13) — dep list now shows `tokio` + `bytes` + `dashmap` + `object_store` (default-features-off) under `io`; `io-aws` enables `object_store/aws` (no direct `aws-sdk-s3` / `aws-config`); API sketch shows conversion traits and `S3SourceBuilder`; R13 updated with the `object_store` thin-wrap decision.
  - Amended [`docs/TECH_DEBT.md`](../TECH_DEBT.md) TD-008 — migration step 1 now "add `object_store` with `default-features = false`"; steps 3-4 split by text-vs-binary transport path; step 6 notes AWS SDKs come in transitively via `object_store/aws`.
- Updated `STATUS.md §1 / §2 / §4 / §5 / §7` to reflect the new phase state, reconciliation item G, open-question file index, checkpoint narrative, and lessons L8 / L9.
- **Sixth pass in the same session (model + entities end-to-end closure)**:
  - Conducted a systematic Q&A sweep covering every remaining design-level ambiguity in the model surface. 18 ratifications landed in a single arc:
    1. Unified `Relationship` struct used at root `relationships:` AND `JoinsetBody.relationships:`; YAML tag `join_type:` (renamed from `type:`), `keys:` as list of `{from, to}` equi-pairs (both sides `SemanticExpr`), optional residual `filter:` predicate, required `cardinality:`, optional `directionality:` (default `bidirectional`).
    2. `JoinType` roster trimmed to `{Inner, Left, Right, Full}`, `#[non_exhaustive]`; `AsOf` explicitly deferred.
    3. Shared Semantics pools at root (`dimensions:` / `measures:` / `metrics:`) + per-DataKind inline-or-`ref` grammar; narrow overridable fields at reference sites (`expr`, `filters`); all other attributes immutable from root-pool declaration.
    4. Orphan policy: every Semantic — root-pool or inline — must physically bind at least once (path to a `Dataset` leaf).
    5. `TemporalShape` type hierarchy — `TemporalShape` struct with `kind: TemporalShapeKind` and `grain: Option<Grain>`; `TemporalShapeKind` enum wrapping `TimeseriesBody` / `EventsBody` / `SnapshotBody` / `ScdBody`; all `#[non_exhaustive]`.
    6. Collapsed YAML wrapper for `TemporalShape` (`extras.temporal.<variant>:` + sibling `grain:`); `grain:` required on leaf `Dataset`; forbidden on any `ComplexDataKind`.
    7. Grainset children must each author `extras.temporal.grain:` explicitly — shape can cascade from the Grainset parent, grain cannot (SR-E-8).
    8. `ScdType` trimmed to `{Type1, Type2}`; other historical variants deferred.
    9. Filter taxonomy — `DataKindFilter` (DataKind-level, pre-aggregation, user-facing predicate) distinct from `AggregationFilter` (Measure / Metric-level, conditional aggregation); no cross-reference (SR-E-11).
    10. `DimensionType` roster kept at six: `{Temporal, Categorical, Binary, Geo, Bucketed, Metadata}`, `#[non_exhaustive]`; per-variant body shapes pinned (`TemporalDimensionBody.grains`, `BucketedDimensionBody.buckets`, `MetadataDimensionBody.source`).
    11. `AggregationType` expanded to nine variants: `{Sum, Avg, Count, CountDistinct, Min, Max, Median, StdDev, Variance}`, `#[non_exhaustive]`.
    12. `AdditivityType` `{Full, Semi(SemiAdditivity), Non}` where `SemiAdditivity { axes, strategy }` uses compact `axes:` / `strategy:` YAML (no trailing `semi:` wrapper).
    13. `Measure.agg:` required at declaration; `Measure.expr:` optional (horizontal-only transform); `Metric.expr:` required; `Metric.agg:` optional (expression itself carries aggregation intent); `data_type:` mandatory at declaration AND immutable across root-pool / `ref` sites (SR-E-10 / SR-E-12).
    14. `Keys` structure `{primary, unique, foreign}` kept; `columns:` values are bare Semantic names resolved via `semantic_mapping` at compile time.
    15. `AiContext` trimmed to four core fields: `synonyms` (alias map), `description`, `examples`, `unit`; `#[non_exhaustive]`.
    16. `SemanticMapping` value = three variants: `Column(String)` / `Literal(LiteralValue)` / `Expr(PhysicalExpr)`. Single-string YAML dispatches to `Column`; `literal:` / `expr:` keys dispatch to the other two.
    17. `CatalogRef` is now a bare alias string (`#[serde(transparent)]`); the map-form authoring (`{alias, namespace}`) and the reference-site `namespace:` override are dropped. Namespace precedence collapses to `CatalogEntry.default_namespace > provider default`.
    18. `PartitionDef` nested inside `StorageConfig` (removed as a top-level `Extras` field); `ComplexDataKind` requires ≥ 2 children (SR-10); implicit planner-synthesised Joinsets are planner-internal only (never queryable top-level entities); root-level Metric may reference Measures / Dimensions across DataKinds — planner synthesises join path via the relationship graph.
  - Created [`apis/32c_entities.md`](apis/32c_entities.md) as the consolidated canonical entity-types spec. Scope: all shapes above + 12 new `SR-E-*` entity-level diagnostic codes. `32c` is authoritative-for the entity surface; `32` continues to own the root document shape and points to `32c` for entity definitions.
  - Amended [`apis/32_semstrait_model.md`](apis/32_semstrait_model.md) front matter (added `32c` to `refined-by`) + §1 (root YAML example now shows a complete `Relationship` block per `32c §2`) + §1.3 (catalog reference reduced to bare alias, map-form example removed) + §3.2 (`JoinsetBody.relationships: Vec<Relationship>` — unified shape per `32c §2`) + §4 (`Extras` struct: removed `partition_def` as top-level field; added `StorageConfig` shape with nested `partition_def`; collapsed-wrapper `temporal:` YAML example) + §4.1 (updated effective-level table: `storage` row now acknowledges nested `partition_def`; `temporal:` split into shape vs grain rows with SR-E-7 / SR-E-8 references) + §4.2 (variant-intrinsic fields: `Relationship` shape points to `32c §2`; `SemanticInterface` authoring grammar points to `32c §1`) + §5.1 (`SemanticMapping` authoring grammar: three-variant enum + three-way YAML dispatch) + §6 (added SR-10 `validate.complex-data-kind-insufficient-children`; footer pointer to `32c §11` for `SR-E-*`) + §7 (`JoinsetBody.relationships` row: `Vec<Relationship>`) + §11 (`32c` entry at the top of the child-docs pointer table).
  - Amended [`apis/32b_catalogs_yaml.md`](apis/32b_catalogs_yaml.md) §4 — `CatalogRef` is now `#[serde(transparent)]` struct around `alias: String`; dropped the map-form authoring example; added §4.1 "Resolution precedence" (two tiers: `CatalogEntry.default_namespace > provider default`) and §4.2 "Why no reference-site namespace override" (rationale + future-extension disclaimer).
  - Amended [`data-kinds/26_nesting_matrix.md`](data-kinds/26_nesting_matrix.md) — renamed §2 to "Rules (R1, R2, R3)"; added §2.3 R3 (`ComplexDataKind` ≥ 2 children) with counting-rule table and example; updated `JoinsetBody.relationships` field in §2.2 to `Vec<Relationship>` per `32c §2`; updated §1 legend to acknowledge R3; added R3 row to §5 enforcement table; added new §6 "Related Entity-Level Rules (pointer)" linking to `SR-E-7` / `SR-E-8` in `32c §3.3` / §3.4 for grain-on-complex and Grainset-child grain-required rules.
  - Updated [`STATUS.md`](STATUS.md) §1 / §2 / §4 / §5 — new phase narrative, new reconciliation item H (canonical entity types), new 32c entry in open-questions index, this sixth-pass checkpoint.
- **Seventh pass in the same session (Phase-3 mechanical cascade, 2026-04-17)**:
  - Systematic pass over every pre-`32c` document to apply the ratified renames and scope corrections without re-opening decisions. No new design choices; only alignment.
  - Strengthened reconciliation pointers at the top of [`foundations/15_mapping_and_binding.md`](foundations/15_mapping_and_binding.md), [`foundations/17_temporal_shape.md`](foundations/17_temporal_shape.md), [`data-kinds/20_taxonomy.md`](data-kinds/20_taxonomy.md), [`data-kinds/21_dataset.md`](data-kinds/21_dataset.md), [`data-kinds/22_grainset.md`](data-kinds/22_grainset.md), [`data-kinds/23_unionset.md`](data-kinds/23_unionset.md), [`data-kinds/24_joinset.md`](data-kinds/24_joinset.md), [`data-kinds/25_applicability_matrix.md`](data-kinds/25_applicability_matrix.md) — each now names the specific `32c` / `32` / `26` sections that supersede its body, enumerates the shape changes (rename roster, v1 roster trim, AsOf descope, R1 / R2 / R3 rules), and retains the doc's surviving authoritative-for items (strategy contracts, error-code allocations).
  - Applied light-touch reconciliation notes at the top of [`foundations/11_names_and_scopes.md`](foundations/11_names_and_scopes.md), [`foundations/12_nesting_policy.md`](foundations/12_nesting_policy.md), [`foundations/14b_expression_resolution.md`](foundations/14b_expression_resolution.md), [`foundations/16_composition.md`](foundations/16_composition.md); updated front-matter `refined-by:` and `authoritative-for:` lines to point at `32c` where the authoritative shape has moved.
  - Fixed the one explicit stale-scope line in [`data-kinds/21_dataset.md`](data-kinds/21_dataset.md) — "Type1, Type2, Type6 at v1 surface" rewritten to "`{Type1, Type2}` per `32c §3.3`" with post-v1 deferral for `Type0`/`Type3`/`Type4`/`Type5`/`Type6`.
  - Updated [`00_overview.md`](00_overview.md) §4.1 glossary — `Binding` / `SemanticMapping` / `TemporalShape` / `JoinType` / `PhysicalExpr` entries rewritten to match `32c` canonical surface. Naming-convention note clarified to call `SemanticMapping` the current name with the historical `ColumnMapping` marker noted.
  - Updated [`registry/README.md`](registry/README.md) — `temporal_shape_mapping` / `join_types_mapping` rows trimmed to v1 rosters (`ScdType` `{Type1, Type2}`, `JoinType` `{Inner, Left, Right, Full}`) with `AsOf` / `Type0`–`Type6` labelled post-v1.
  - Added a `[TD-MAP-METADATA-FOLD]` tech-debt marker at the top of `15` — the 4-variant `ColumnMappingValue{Column, Literal, Computed, Metadata}` vs. 3-variant `SemanticMappingValue{Column, Literal, Expr}` roster change (with `Metadata` folded into `Expr`) is the single shape change that requires a targeted body rewrite in a follow-up pass. Flagged explicitly because it's the only spot where the mechanical cascade hit a decision it couldn't safely automate; the follow-up ratification item is "how to express `path.token` / `partition.level` metadata extraction under the `Expr(PhysicalExpr)` variant at the YAML sugar level."
  - Annotated seven open-Q items as **CLOSED** with closure notes pointing at `32c`:
    - `Q-UNI-002` (`UnionMode::Distinct` in v1) — closed; v1 roster `{All, Unique}` per `32c §2`; `Distinct` renamed to `Unique`.
    - `Q-UNI-009` (single-child Unionset acceptance) — closed by R3 (`ComplexDataKind` ≥ 2 children); `[TD-UNIONSET-SINGLE-CHILD]` retired.
    - `Q-GRN-004` (Grainset-of-Grainset nesting) — closed by R2 (no same-variant self-nesting); `[TD-GRAINSET-NESTED]` retired.
    - `Q-GRN-006` (single-child Grainset degeneracy) — closed by R3; `[TD-GRAINSET-SINGLE-CHILD]` retired.
    - `Q-TEMPORAL-002` (SCD payload shape: per-subtype vs flat-fields) — closed; `32c §3.3` ratifies flat fields with v1 roster `{Type1, Type2}`.
    - `Q-TEMPORAL-005` (default-current without `current_flag_dim`) — closed; v1 `ScdBody` drops `current_flag_dim` entirely.
    - `Q-TEMPORAL-006` (`Scd::Type0` append-only enforcement) — closed; `Type0` descoped from v1 roster.
    - Updated the Q-TEMPORAL status table at the bottom of `17_questions.md` accordingly.
  - Net effect: zero new decisions, every pre-`32c` doc now has a strong v1-surface reconciliation pointer, seven open questions retired, one tech-debt marker added for a follow-up decision. The model surface is mechanically aligned with `32c` ratifications.
- **Eighth pass in the same session (documentation consolidation, 2026-04-17)**:
  - **Goal**: user-driven review for squashing possibilities — "one source of truth per topic, avoiding duplication, spark pointers and indexes for optimized access for human/ai agents." Zero new design decisions; pure structure optimization.
  - **Promotion**: `apis/32c_entities.md` → [`foundations/18_entities.md`](foundations/18_entities.md) (git mv preserving content + history). Rationale: the types ratified in `32c` (Relationship, JoinType, TemporalShape, Dimension/Measure/Metric, filter taxonomy, AiContext, Keys, SemanticMappingValue, `SR-E-*`) are structurally foundational — they cross-cut every `2x` data-kind, every `3x` api, and every planner/adapter consumer. Per the directionality rule in `00 §8`, canonical definitions belong in the lowest-numbered doc that owns them; entity types belong in the foundations layer (`1x`), not at an `apis/32c` sibling. Section numbering preserved (`18 §N` ≡ former `32c §N`).
  - **Evisceration**: duplicate struct / enum definitions for the 18-owned entity types were purged from every other design doc that had accumulated them:
    - `foundations/16_composition.md` — `Relationship` / `JoinType` / `Cardinality` / `Directionality` struct defs removed; `authoritative-for` narrowed to composition semantics (path traversal, fanout analysis, `ComposedSemanticInterface` construction) with explicit "(struct shape owned by `18 §2`)" disclaimers.
    - `foundations/17_temporal_shape.md` — the pre-consolidation `pub enum TemporalShape` + `pub enum ScdSubtype` (Type0–Type6) blocks in §2.3 removed entirely; §2.3 now carries a "canonical shape pointer" to `18 §3` + design-rationale prose (per-subtype payload vs flat-fields, non-exhaustive discipline). The body of §2 retains the **post-v1 forward-reference discussion** of Type0 / Type3–Type6 as semantic reference material (what each subtype *means*), explicitly labelled post-v1. `authoritative-for` narrowed to planner-level semantics (shape × grain matrix, AsOf forward-reference design, advisory-warning roster, `VALID_E` / `COMP_E` / `PLAN_E` code allocations).
    - `foundations/15_mapping_and_binding.md` — `ColumnMapping` → `SemanticMapping` rename applied inline; `SemanticMappingValue` enum roster delegated to `18 §10`. §1's reconciliation banner trimmed; `authoritative-for` narrowed to the compile-time Binding process + `PhysicalSource` taxonomy + the `ResolvedColumnMapping` SemanticManifest shape (SemanticManifest layer retains the `Resolved*` name per convention).
    - `foundations/11_names_and_scopes.md` — `authoritative-for` narrowed to *roles and field layouts* (Dimension / Measure / Metric ratified in `18 §4` / `§5` / `§6`); explicit "(struct shape owned by `18 §N`)" disclaimers on Additivity / Dimension / Measure / Metric entries.
    - `foundations/13_types_and_grain.md` — `DimensionType` enum block removed; §4 now owns the *planner-level and authoring-level semantics* of each variant (enum roster + body struct shapes ratified in `18 §4.1`).
    - `foundations/14b_expression_resolution.md` — duplicate `RelationshipId` newtype removed; canonical home is `18 §2.1` (confirmed `Ord` / `PartialOrd` derives for deterministic graph traversal).
    - `apis/33_semstrait_manifest.md` — duplicate `RelationshipId` newtype removed; pointer to `18 §2.1`.
    - `apis/32_semstrait_model.md` — `SemanticMappingValue` redirected to `18 §10`; `refined-by:` trimmed to downstream-only per directionality rule (upstream cross-refs preserved in `prereqs:` + §11 "Pointers to Child Docs" navigation table).
  - **Grain duplicate discovered and fixed**: `18 §3.5` had silently introduced a `pub enum Grain` block with a `Second` variant not present in `13 §3.1`. Per the precedence rule, `13` is canonical (front-matter + glossary + directionality all align). The block was replaced with a pointer to `13 §3.1`; `18`'s front-matter `authoritative-for:` updated to clarify Grain is consumed but not re-ratified.
  - **RelationshipPath duplicate discovered and fixed**: `16 §5.2` had a `pub struct RelationshipPath(pub Vec<RelationshipId>)` block that duplicated `14b §4.5`'s canonical newtype. `14b` is the canonical home (authoritative-for includes "the `RelationshipPath` struct at the shared-vocabulary layer"); `16 §5.2` was converted to a pointer.
  - **Open-questions consolidation**:
    - Swept all 29 `questions/open/*.md` files. Added "Status summary" callouts to the top of files with CLOSED items (`23_questions.md` — Q-UNI-002 / Q-UNI-009 CLOSED; `22`, `17`, etc.). No bulk deletion — retained entries preserve closure rationale.
    - Created [`questions/open/registry_questions.md`](questions/open/registry_questions.md) as a navigation-only aggregate index covering all 35 open questions across the three registry sidecars (`functions_mapping_questions.md`, `join_types_mapping_questions.md`, `temporal_shape_mapping_questions.md`). Sidecars retained as canonical for detail bodies; index provides single-file overview with status rollup, tech-debt tag families, and cross-catalog coordination items.
    - Folded standalone [`questions/open/24_questions.md`](questions/open/24_questions.md#post-v1-shape-hint-clusters-folded-in-2026-04-17) into `24_questions.md §Post-v1 shape-hint clusters` as Q-24-09 (JoinAssociativity) + Q-24-10 (star / snowflake / 3NF shape-tag vocabulary). Historical references to the retired sidecar remain as pointers to the new section.
  - **New top-level index**: created [`docs/design/INDEX.md`](INDEX.md) — sibling to `00_overview.md`. Provides (a) alphabetical topic → canonical-doc lookup for every named concept; (b) folder map matching `00 §6.2`; (c) duplication guard table listing concepts owned by exactly-one-doc with per-concept consumer patterns; (d) crate-surface mirroring convention (explicit rule for when `3x` api docs may mirror a `1x` foundations definition); (e) aggregated open-questions index (one row per sidecar); (f) maintenance rules for keeping the index in sync with `00 §4`. Section cross-refs audited for accuracy (e.g., AdditivityType → `18 §5.2`, ScdType → `18 §3.1`, Keys → `18 §9`).
  - **New precedence rule**: [`00_overview.md §4.4`](00_overview.md) ratifies the conflict-resolution order for apparent disagreements between docs: (1) `authoritative-for:` front matter wins → (2) directionality rule (lower-numbered doc) fallback → (3) §4 glossary fallback. Unresolvable conflicts are design bugs, not intentional overrides. `00 §4` opens with a scan-optimized-index note pointing to `INDEX.md`.
  - Net effect: zero new design decisions, one canonical home per cross-cutting entity type, two discovered drifts (`Grain` roster + `RelationshipPath` duplicate) fixed, one new index file + one new precedence rule ratifying conflict resolution going forward. The documentation structure is hardened against duplicate-struct accretion; future readers have a compact navigation surface (`INDEX.md`) that stays in lockstep with `00 §4`.
- **Ninth pass (questions/ directory restructure, 2026-04-27)**:
  - **Goal**: user-driven visibility improvement — *"to make it more visible, can we a bit restructure ours design docs for questions. let's rename open_questions dir into 'questions', there we will have 'open' & 'closed' directories. Put md docs respectfully, so it will be more visible for you and authoring human."* Zero new design decisions; pure structure restructure.
  - **Closure survey**: of the 30 sidecars (29 numbered + the retired `joinset_shape_semantics.md` already folded in the eighth pass), exactly one — [`31b_io_questions.md`](questions/closed/31b_io_questions.md) — has every Q-ID resolved (every heading carries `**RESOLVED**` / `**CLOSED**` / `**RETIRED**`; the file's opening line is `# 31b — Open Questions (all resolved)`). All 28 others have at least one unresolved Q-ID, including six files (`23`, `17`, `22`, `32`, `31b_io`, `registry`) that have a mix of CLOSED + open items. Per the user's file-level partitioning choice, mixed files stay in `open/`.
  - **File-level moves (29 `git mv`)**:
    - `docs/design/open_questions/` → `docs/design/questions/{open,closed}/`.
    - 28 files to `questions/open/`: `11_constraints_deferred.md` (kept name); `14b`, `15`, `16`, `17` (foundations); `20`–`25` (data-kinds); `30`–`39` (apis, except `31b`); `40`–`42` (implementation); `registry_questions.md` (aggregate index); `functions_mapping_questions.md`, `join_types_mapping_questions.md`, `temporal_shape_mapping_questions.md` (registry sidecars).
    - 1 file to `questions/closed/`: `31b_io_questions.md`.
    - Filename suffix `<doc>_open_questions.md` → `<doc>_questions.md` across all 28 numbered + named sidecars (status now comes from the parent directory, no longer carried in the filename). The exception (`11_constraints_deferred.md`) had no `_open_questions` suffix to drop and keeps its name.
    - Old directory `open_questions/` removed.
  - **Mechanical reference sweep** (one perl `find -exec` pass over every `.md` file in `docs/design/` + `CLAUDE.md`):
    - Path-prefixed: `((?:\.\./)*)open_questions/...` → `${1}questions/open/...` or `questions/closed/31b_io_questions.md` (closed special-case first).
    - Bare filename: `\b<doc>_open_questions(\.md|\b)` → `<doc>_questions(\.md|\b)`.
    - Front-matter `doc:` fields updated automatically by the same pass (22 of 29 sidecars carry one; the other seven have no front matter — left as-is).
    - Five residual hand-fixes for bare-token references where `open_questions` lacked the leading `_` (e.g. `35 open_questions Q-IR-010`, `14b open_questions OQ-7`, the brace-glob `{20..25}_open_questions.md` in STATUS.md, the `<catalog>_open_questions.md` template in `registry/README.md`, the doc-map intro line in `INDEX.md`). All five rewritten by hand.
  - **INDEX.md**: folder-map updated to show the new two-subfolder layout (`open/` + `closed/`); the single "Open questions" table split into two tables (`questions/open/` active backlog, `questions/closed/` historical record); the "Renames landed" cross-reference section appended a new "ninth pass (2026-04-27)" entry summarizing the directory rename, suffix drop, and mechanical sweep.
  - **STATUS.md**: §1 phase line + phase-map row added for the ninth pass; §5 carries this checkpoint; lesson L12 below.
  - **Verification**: zero residual `open_questions` matches in any *active* reference context (every file path, every link, every front-matter field has been rewritten); the only remaining `open_questions` tokens are inside this STATUS.md ninth-pass checkpoint and the INDEX.md "Renames landed" log entry, which describe the rename historically and are intentional. Every `doc:` front-matter field matches its new filename. Six files referencing `<doc>_questions` shorthand (e.g. `36_questions Q-IR-010` from a hand-fixed residual) follow the new convention.
  - Net effect: zero new design decisions, zero content changes inside any sidecar. The active backlog is now a single ls of `questions/open/` (28 files) and the archive is a single ls of `questions/closed/` (1 file). Future closures move whole files from `open/` → `closed/` in their final consolidation pass; per-Q-ID closure happens in place inside the file with the existing "Status summary" callout pattern.
- **Tenth pass (metadata-derived Dimension authoring surface ratification, 2026-04-27)**:
  - **Goal**: resolve the `[TD-MAP-METADATA-FOLD]` follow-up — the only ratification item flagged as deferred from the seventh pass — by closing every clause around metadata-derived Dimensions: authoring surface, compile mechanic, error model, manifest representation, planner access pattern, coverage taxonomy, and architectural layering. Driven by the user's `find out what is open in design for model and start conversation session to cover e2e everything related to model itself, deferring constraints DSL` directive.
  - **9 core clauses + 6 derived clauses ratified through interactive Q&A**:
    1. **Authoring surface** — author writes the recipe on the Dimension type itself (`type: { metadata: { path: { token: N } } }`); no `semantic_mapping:` YAML for metadata Dimensions; compile synthesizes the binding entry.
    2. **v1 scope** — path-token extraction only; partition extraction (`partition.level: N`) deferred to v2 (compile guard `COMP_E_0322`).
    3. **Per-source resolution** — each `PhysicalSource` resolves to exactly one typed `LiteralValue` per metadata Semantic; values can differ across sources; eagerly evaluated at compile.
    4. **SemanticManifest storage** — recipe (global to Binding) on `ResolvedColumnMapping.metadata: HashMap<SemanticsName, MetadataDimensionRecipe>`; per-source resolved `LiteralValue`s on `ResolvedPhysicalSource.metadata_values: HashMap<SemanticsName, LiteralValue>`.
    5. **Error model** — fail-fast at compile for every applicability / extraction / cast failure: `COMP_E_0311 MetadataTokenOutOfRange`, `COMP_E_0312 MetadataTokenOnNonFileSource`, `COMP_E_0320 MetadataTokenEmpty`, `COMP_E_0321 MetadataCastFailed`, `COMP_E_0322 MetadataPartitionDeferredV2`. Partition codes `COMP_E_0313 / 0314` reserved for v2 (no v1 emitter).
    6. **Cast policy** — layer-3 mechanic returns raw `String`; compile applies `Cast` to declared `data_type:`; cast failure is fail-fast.
    7. **path_token semantics** — 0-indexed, scheme-stripped, raw segment (not `=`-suffix value).
    8. **Coverage taxonomy** — `Coverage::Metadata` added as a 4th variant of `CoverageVariant` (`{Native, NullFill, Derived, Metadata}`); used when the Binding's `SemanticMappingValue` for the Semantic is `Metadata(...)`.
    9. **Three-stratum architecture** — formalized the `SemanticExpr` (layer 1, logical, `14`/`14b`-owned, supports constant folding / partial evaluation) / `PhysicalExpr` (layer 2, lowered SQL-equivalent, `14`-owned) / compile-time-mechanic (layer 3, `15`-owned, e.g. `path_token`) layering. `path_token` is layer 3 — not a `PhysicalExpr` variant and not a `14a` registry function.
    - **Derived clauses (D1–D6)** ratifying the implementation shapes: `MetadataDimensionRecipe` / `MetadataExtraction` struct definitions in `18 §10.4`; `ResolvedPhysicalSource.metadata_values` field shape in `15 §7.6`; new error codes; v2-deferred banner approach for partition text in `13 §4.7` / `15 §8.2-§8.4`; three-stratum subsection in `15 §1` banner; Q-MAP retirement plan (Q-MAP-007/008 closed, Q-MAP-009 deferred to v2).
  - **Documentation edits landed**:
    - **`docs/design/foundations/18_entities.md` §10**: 3-variant `SemanticMappingValue` roster expanded to 4-variant (`Column` / `Literal` / `Expr` / `Metadata(MetadataDimensionRecipe)`); new §10.4 defines `MetadataDimensionRecipe { extraction, data_type }` + `MetadataExtraction::Path { token: u32 }` (`#[non_exhaustive]` for v2 partition arm); compile-synthesis note explicit.
    - **`docs/design/foundations/15_mapping_and_binding.md`**: top banner reversed `[TD-MAP-METADATA-FOLD]`; §1.1 list updated for v1 path-only scope + layer-3 stratum entry; §5.1 4-variant roster; §5.5 fully rewritten as the new compile mechanic (author surface + per-source resolution flow + constant-folding feed); §5.7 Metadata bullet rewritten; §6.1 `Coverage::Metadata` variant added with description; §6.2 dispatch updated; §7.2 `metadata` HashMap value type changed to `MetadataDimensionRecipe`; §7.3 construction simplified (4-variant direct-route); §7.4 planner access pattern includes per-source `metadata_values` lookup; §7.6 introduces `ResolvedPhysicalSource` struct with `metadata_values` field; §8 reorganized — §8.0 v1-scope banner, §8.1 layer-3 `path_token` mechanic + cast policy + 4-error code table, §8.2/§8.3/§8.4 marked v2 design parking; §10.4 step 4 broken into substeps including new step 4.0 metadata-recipe synthesis; §10.5 documents per-source `metadata_values` population; §10.6 materialization includes `ResolvedPhysicalSource` wrapping; §11.1 adds `COMP_E_0320` / `COMP_E_0321` / `COMP_E_0322` and marks `COMP_E_0313` / `COMP_E_0314` as v2-deferred; §11.5 governance count updated (21 codes + 7 advisory); §13 R18/R22 updated; R28/R29 closed (Q-MAP-007/008); R30/R31/R32 marked v2-deferred; new R43–R48 added; §15 vocabulary anchors include `MetadataDimensionRecipe` / `MetadataExtraction` / `ResolvedPhysicalSource` / `path_token`.
    - **`docs/design/foundations/16_composition.md` §8.2/§8.3**: `CoverageVariant` enumeration extended with `Metadata` description; fold rule includes `Some(Metadata) => Metadata` case; Q14 row updated.
    - **`docs/design/foundations/13_types_and_grain.md` §4.7**: v1-scope banner added (path-only); path-segment write-up tightened with cast-to-declared-`data_type` reference; partition-segment write-up explicitly marked v2 design parking.
    - **`docs/design/questions/open/15_questions.md`**: reconciliation banner updated (4-variant roster, `[TD-MAP-METADATA-FOLD]` resolved); Q-MAP-007 closed (Option A ratified); Q-MAP-008 closed (raw segment ratified); Q-MAP-009 deferred to v2 (partition non-goal in v1).
    - **`docs/design/STATUS.md`**: phase-3 row updated to mark `[TD-MAP-METADATA-FOLD]` resolved; this checkpoint added; next-session block updated.
  - **Process correction (mid-session)**: user flagged that the assistant was using internal alphanumeric indexing (e.g. `M1-α`, `M1-β`) in user-facing communication, which was unreadable. Fix: switched to plain-language topic names + actual Q-IDs from source files; `CLAUDE.md` Rule 4 added codifying that design / spec edits are subject to the same wait-for-confirmation discipline as code changes, with approval at clause-level (a directional pick does NOT authorize derived implementation clauses — validation rules, error codes, edge cases each require their own decision).
  - Net effect: the `[TD-MAP-METADATA-FOLD]` follow-up is fully resolved with all clauses ratified and documented; the v1 model surface no longer carries any flagged tech-debt markers; metadata-derived Dimensions are end-to-end specified (authoring → compile mechanic → manifest → planner). Three-stratum architecture now anchored as a cross-doc reference point (consumed by `14`/`14b` for partial-evaluation work that will land later). Six new error codes allocated within the `COMP_E_0300-0399` range. Six new ratifications (R43–R48) added to `15 §13`'s ratified-decisions index.

**Next-session starting point**:

1. Read [`INDEX.md`](INDEX.md) **first** for scan-optimized topic → canonical-doc lookup, then `00_overview.md` + `STATUS.md` (mandatory) + `apis/32_semstrait_model.md` + `foundations/18_entities.md` + `apis/31b_semstrait_core_io.md`.
2. Model + entity surface is fully closed mechanically; documentation is deduplicated end-to-end. The previously-flagged `[TD-MAP-METADATA-FOLD]` follow-up was **resolved on 2026-04-27** — see the metadata-derived Dimension authoring surface ratification below. Four larger deferred threads remain:
   - **`[TD-MAP-METADATA-FOLD]` — RESOLVED (2026-04-27).** The fold of `Metadata` into `Expr` is reversed; `Metadata` is restored as a distinct 4th variant of `SemanticMappingValue` (per `18 §10` / `15 §13 R48`). The author surface is the Dimension's `type: { metadata: { path: { token: N } } }` block, not a `semantic_mapping:` YAML sugar. v1 scope is path-token only; partition extraction deferred to v2. The compile flow eagerly resolves per-source `LiteralValue`s at compile time and stores them on `ResolvedPhysicalSource.metadata_values`. Three-stratum expression model (SemanticExpr / PhysicalExpr / compile-time-mechanic, with `path_token` as the v1 layer-3 inhabitant) was anchored in `15 §1`. Touched: `15` (banner / §1.1 / §5 / §6 / §7 / §8 / §10 / §11 / §13 / §15), `18 §10`, `16 §8`, `13 §4.7`, `15_questions.md` (Q-MAP-007/008 closed; Q-MAP-009 deferred to v2). New error codes: `COMP_E_0320 MetadataTokenEmpty`, `COMP_E_0321 MetadataCastFailed`, `COMP_E_0322 MetadataPartitionDeferredV2`. New ratifications: R43–R48 in `15 §13`.
   - **Per-variant doc body polish** — the pointers in `21`–`24` are strong enough that readers land on `18` / `32` for authoritative shapes, but each doc still has its pre-`18` Rust-struct / YAML examples inline. If future readers find the dual-surface confusing, a follow-up pass could collapse each variant doc to "strategy + error-code-roster + worked example" and delete the pre-`18` struct prose. Not urgent.
   - **Item C — Adapter / Catalog framing** — ratify the feature-gated-modules framing in `36`, `37`, `39`, `42`; edit or replace sections that describe per-engine / per-provider crates.
   - **Item E — Constraints session** — resume from [`questions/open/11_constraints_deferred.md`](questions/open/11_constraints_deferred.md); pick up three open ratification items (`aggregation:` sub-block, key naming, `filter:` placement). Constraints will slot **into** `Dimension` / `Measure` / `Metric` entries per `18` but the DSL shape is separate work.
   - **TD-008 implementation** — the I/O design is ratified; the actual code migration (land `core::io` module, refactor `semstrait-manifest::io`, migrate CLI / builder call sites) is now unblocked.
3. Constraints (item E) still waits for its dedicated session. Per-variant doc body polish, Item C, and TD-008 implementation can be interleaved.
4. **Documentation discipline (new this pass)**: any new cross-cutting type or rule must (a) have exactly one `authoritative-for:` home, (b) be cited from `INDEX.md` *before* the commit lands, and (c) respect the directionality rule in `00 §8`. If a duplicate struct / enum block is introduced to a second doc as a reader convenience, it must cite the canonical home in-line per the [`INDEX.md` Crate-surface mirroring convention](INDEX.md). When drift is observed, resolve via the [`00 §4.4` precedence rule](00_overview.md).

---

## 6. Session update rule

STATUS.md is updated at the end of each working session. Discipline:

1. Agent proposes the diff to the human at session close.
2. Human approves or edits inline.
3. Approved STATUS.md change is committed alongside the spec edits that caused it.

Changes to §5 (last checkpoint) are mandatory on every spec session. Changes to §§1–4 are only when the underlying state actually changed.

---

## 7. Lessons learned (append-only)

Short notes on recurring failure modes observed during this exercise. New entries append; existing entries are not edited.

- **L1 (2026-04-17)** — The spec tree must ground every abstraction in current code. Introducing parallel models (e.g. `Binding` where only `ColumnMapping` exists; a unified `data_kinds:` YAML tag where only per-variant plural tags exist; a generic constraint framework with reserved future carriers where only Measure/Metric carry them) is the recurring failure mode. The job is *describe, clean, and name what exists* — not propose alternate abstractions to sugar over existing shapes. Any abstraction that is not in code must be flagged as a proposed extension with explicit justification, not smuggled in as a "natural" generalization.
- **L2 (2026-04-17)** — Prose spelling discipline: YAML-facing noun ≠ internal Rust variant. Conflate them and cross-doc references drift. Maintain both spellings explicitly.
- **L3 (2026-04-17)** — When drafting against a feature-gated crate, distinguish module-level feature flags from crate-level separation. The former is a v1 concern; the latter is a migration-plan concern.
- **L4 (2026-04-17)** — Spec documents hold design definitions, not decision logs. Rationale, "what this doc ratifies / does NOT ratify," divergence analyses, and open-item lists belong in `STATUS.md` or per-doc open-question files, not at the top of the spec itself. Keep the spec body pure: structure, types, grammar, rules, diagnostics.
- **L5 (2026-04-17)** — Generics are a tool, not a goal. A generic umbrella like `BodyType<Public, Nested>` reads worse than two named concrete structs when the two forms differ in field set. Prefer concrete Public / Nested types with per-variant bodies; reach for generics only when two forms share a field set exactly and the common field set outweighs the naming cost.
- **L6 (2026-04-17)** — When a "generic base" contradicts "per-variant rules" (e.g. a single `ComplexDataKind` struct that simultaneously enumerates concrete children AND bans self-nesting per arm), switch to an enum-of-bodies shape. Structural constraints that differ across variants belong on the variant, not on the base; the base should only hold genuinely universal fields.
- **L7 (2026-04-17)** — "Is-a" relationships (e.g. "a `Dataset` is a `DataKind`") belong in the type system, not in prose. Composition (`has-a`) alone cannot express them: a caller cannot write a generic function bounded by "is a data kind" when `DataKind` is a struct. The remedy is a sealed trait hierarchy: a base trait for the universal interface and marker / accessor sub-traits for each classification axis (structural vs behavioral in our case). Marker traits are cheap — their value is the bound itself. Pair the trait hierarchy with view enums so heterogeneous iteration still has an exhaustive `match` against concrete variants; the views implement the common trait subset, letting the same bound work on a single concrete, a `&dyn Trait`, or a `ViewRef<'_>`.
- **L8 (2026-04-17)** — When a shared infrastructure piece (I/O, diagnostics, config) has to live somewhere, the choice between "new sibling crate" and "new module in `core` behind a feature flag" is a real trade-off, not a reflex. A sibling crate *feels* cleaner but adds a workspace edge, a `Cargo.toml`, a CI matrix cell, and forces every downstream to declare a second dependency — with no additional isolation when every crate would end up depending on it anyway. A feature-gated `core` module preserves core's zero-runtime-dep posture *only if* the feature is strictly opt-in (`default-features = false` restores zero deps) AND the module exposes only the transport vocabulary — domain wrappers (`load_model`, `load_manifest`, …) must stay in their owning crates, otherwise you pull every downstream into core's dependency graph. Rule: transport in core (gated, opt-in); domain in owning crate; no wrapper fan-in. When in doubt, re-run the test: "if I set `default-features = false` on every crate in the workspace, does `cargo tree` show zero runtime deps on core's `io` module?" — if no, the placement is wrong.
- **L9 (2026-04-17)** — When the broader ecosystem already ships a battle-tested abstraction for a commodity concern (I/O transport, HTTP, async-runtime glue), the default answer is *thin-wrap*, not *reinvent*. The hand-rolled version always looks simpler on day one — then collects retry policy, multipart uploads, credential chains, proxy config, streaming decoders, and a dozen S3-compatibility bugs over the next two years. Thin-wrapping `object_store` for `core::io` replaced ~300 LOC of glue with trivial delegation and unlocks GCS / Azure / HTTP as ~30-LOC additive MINORs each. Two discipline rules survive the wrap: (1) the ecosystem crate is an *internal* detail — never re-exported except through one documented escape hatch (`S3SourceBuilder::with_object_store_builder`); (2) our trait surface stays small enough that swapping the wrapped crate later is an internal-only change. The litmus test: "could we swap `object_store` for `opendal` without breaking a single downstream crate?" — if yes, the encapsulation is right. Separately: when asking the user "library depth?" questions, show the *concrete* trade-off table (transitive deps, API stability, license, dep cost under `--no-default-features`) rather than abstract pros/cons — it shortens the decision loop dramatically.
- **L10 (2026-04-17)** — Closing a surface end-to-end benefits from a **consolidation doc** separate from the root doc. `32c_entities.md` was added as a sibling to `32_semstrait_model.md` rather than bloating `32` itself; the separation makes the root doc remain navigable (structure-level truths: root YAML shape, DataKind hierarchy, Extras composition, SR-*) and concentrates the entity-level cross-cuts (Relationship grammar, TemporalShape, Dimension / Measure / Metric shape, filter taxonomy, SemanticMapping value shape) where they compose cleanly. The pattern: *root doc for container, sibling doc for contained*. Applying the same split prophylactically will help when Constraints lands (its natural home is a new `32d_constraints.md` referenced from `Measure` / `Metric` definitions in `32c`), and again when per-DataKind docs (`21`–`24`) get their rewrite (each will be thinner now that it can reference `32c §N` instead of redeclaring Dimension / Measure / Metric shapes). Second rule: when the ratified decisions include both *authoritative* shapes (e.g. `Relationship` struct fields, `AggregationType` roster) and *cross-cutting* rules (e.g. "grain-forbidden-on-complex", "filter-kinds-disjoint"), number the rules in their own independent series (`SR-E-*` separate from the root doc's `SR-*`) so later additions in either series don't perturb the other's numbering.
- **L12 (2026-04-27, ninth pass)** — **Status-bearing filenames and status-bearing directories conflict — pick the directory.** The old layout carried open/closed status in the filename (`<doc>_open_questions.md`) which was correct on day one (one flat directory; the suffix told you the file's purpose) but broke down once individual Q-IDs started closing inside files: a file named `23_open_questions.md` could carry two CLOSED items and twelve open ones, and the filename gave the wrong impression. Three things observed once the directory had ~30 mixed files: (1) finding "what's still open" required opening files one by one; the filename gave no signal beyond "this file has questions." (2) the "all-resolved" file `31b_io_open_questions.md` looked identical in `ls` to the still-active ones — its closure was only visible inside the file's opening line. (3) every cross-doc reference repeated the redundant suffix (`open_questions/15_open_questions.md` says "open" twice). The fix was structural: the directory carries status (`questions/open/` vs `questions/closed/`), the filename carries identity (`15_questions.md`), and `ls` now answers "what's open" / "what's archived" in one glance. Two rules survive the fix: (a) **never encode the same fact in two places** — if status is in the directory, drop it from the filename; if a single piece of metadata varies between files, decide once where it lives (filename vs subfolder vs front-matter) and remove it from the others. (b) **plan for files moving categories** — when a file's status can flip (an `open/` file becomes `closed/` once every Q-ID resolves), the structure must support that movement cheaply. The current structure does: `git mv questions/open/X_questions.md questions/closed/X_questions.md` is one command, history is preserved, every reference inside the file (which uses relative paths to siblings) keeps resolving. Compare to the alternative of splitting closed Q-IDs out into a parallel sibling file: that would have created two files per source doc, and *every* cross-reference would need a "is this in the open or closed file?" disambiguation. The simpler structure is also the more durable one. Third observation (mechanical, but worth recording): a perl `find -exec` pass with ordered substitutions (specific → general, path-prefixed before bare-filename) handled ~250 references in one shot with five hand-fixes for tokens the regex couldn't disambiguate. When a rename is mechanical, treat it as mechanical — write the substitution rules once, run them, then verify with a single `rg` of the old pattern returning zero matches. Don't try to enumerate every reference site by hand; you'll miss some.
- **L11 (2026-04-17, eighth pass)** — A **consolidation doc at the wrong layer accretes drift**. `32c_entities.md` was the right *pattern* (L10) but the wrong *placement*: siting cross-cutting entity struct defs in the `3x` api layer inverts the dependency direction — every `1x` foundations doc that predates it has to reconcile against an `api`-level sibling, and the next author reaching for a struct shape has two canonical homes to choose between. Two concrete consequences observed once the structure grew: (1) two silent duplicate blocks appeared (`18 §3.5` re-declared `Grain` with an extra `Second` variant not in `13 §3.1`; `16 §5.2` re-declared `RelationshipPath` identical to `14b §4.5`), each discovered only during a dedicated audit; (2) `32`'s `refined-by:` grew to include upstream docs (`18`, `15`, `21`, …) because authors reading the file saw `32c` below them and generalized the direction. Three survivable rules: (a) **directionality is structural** — canonical definitions belong in the lowest-numbered doc that owns them, per `00 §8`; a consolidation doc is a presentation concern that sits in the same layer as its owned concepts. (b) **a concept has exactly one `authoritative-for:` home** — `authoritative-for` is a *claim* not an *aspiration*; if two files list the same concept, one of them is wrong and precedence (`00 §4.4`) decides which. (c) **every duplicate code block is a latent drift vector** — the two fixes this pass (`Grain` roster, `RelationshipPath` shape) were zero-risk only because the duplicates happened to match (or nearly match) the canonical home; the next accidental addition could diverge and stay divergent for months. Remedy going forward: whenever a doc needs to show an already-canonical shape for reader convenience (the `3x` api docs exposing foundations types are the dominant case), follow the `INDEX.md` Crate-surface mirroring convention — in-comment pointer to the canonical home, shape must match byte-for-byte, mirror appears once per crate at the `§N Type-level surface` heading. Any violation of those three constraints is a design bug fixable at the canonical home, not the mirror.
- **L13 (2026-04-27, tenth pass)** — **Three strata, three vocabularies — don't conflate them.** When designing metadata-derived Dimensions, the assistant proposed `path_token` first as a `14a` registry function (rejected: registry functions are runtime SQL primitives), then as a `PhysicalExpr` variant (also rejected: `PhysicalExpr` is a lowered SQL expression — same operational layer as the registry, just different vocabulary). Both placements were architecturally wrong. The user's correction surfaced a previously-implicit three-stratum model that now anchors `15 §1.1`: (1) **`SemanticExpr`** — logical layer, operates on Semantic names, supports constant folding and partial evaluation, owned by `14`/`14b`; (2) **`PhysicalExpr`** — the lowered SQL-equivalent layer, operates on physical columns / literals / registry functions, owned by `14`; (3) **compile-time mechanic** — neither expression nor registry, runs once at compile-time to derive values for the manifest, owned by `15`. `path_token` is layer-3 — it is *not* an expression at all, it is a deterministic derivation that evaluates against the source URI before any plan exists. The bug pattern was treating "computes a value" as sufficient evidence for "is an expression": a compile-time URI-segment extraction shares some shape with a runtime function call (parameterized, deterministic, pure) but the temporal binding is different — layer 3 evaluates against compile-time-known inputs (the source URI, the resolved path components), layer 2 evaluates against runtime-known inputs (column values, query parameters). Three rules survive: (a) **temporal binding sorts the layer** — when proposing a new operation, ask "what inputs does this need, and when are they known?" Compile-time-known inputs → layer 3 (mechanic, lives in the model/compile docs). Runtime-known column values → layer 2 (expression, lives in `14`). Pure semantic-name reasoning that gets lowered before runtime → layer 1 (`14b`). (b) **vocabulary lives with the layer** — layer-3 mechanics get bare function names (`path_token`); layer-2 functions get registry IDs and `FunctionRef` plumbing; layer-1 nodes get explicit AST variants. The vocabulary of one layer must not leak into another layer's home doc — layer-3 mechanics don't appear in `14a`'s function tables; layer-2 expressions don't appear in `15`'s compile-flow tables. (c) **a fourth variant beats a clever encoding** — when `Metadata` got folded into `Expr(Cast(path_token(N), data_type))` to keep the `SemanticMappingValue` enum at three variants, it papered over the layer mismatch (`Expr` = layer 2 by definition; metadata extraction = layer 3) and forced `14a` to host a non-runtime function. Restoring `Metadata(MetadataDimensionRecipe)` as a distinct fourth variant ([`15 §13 R48`](foundations/15_mapping_and_binding.md)) made the layering explicit at the type level. Whenever a "natural fold" of a variant into an existing one requires the embedding side to host vocabulary it doesn't otherwise own, the fold is the anti-pattern; carry the variant explicitly. Fourth observation (process): the user-facing impact of getting this wrong was 3 reversed clauses (the original `Metadata`-into-`Expr` fold from the seventh pass, the `14a registry` proposal from earlier in the tenth pass, the `PhysicalExpr` variant proposal from later in the tenth pass) and a `[TD-MAP-METADATA-FOLD]` tech-debt marker that lived in `STATUS.md` for ten days waiting for resolution. The cost of conflating layers is not zero — it accretes as flagged tech debt until a session pulls the layering apart explicitly. Better to name the strata up front and check every new operation against them before introducing it.
